# Removed Configs

Previously included in the setup script but removed.

## Mimestream (removed 2026-04-15)

```bash
defaults write com.mimestream.Mimestream TextSizeAdjustment -int 7
defaults write com.mimestream.Mimestream ShowBadgeForSpam -bool yes
```

Condition: `AppVersion` >= 10
